
在一个被同步的区域内部，不要调用设计成要被覆盖的方法，或者是由外部以函数对象的形式提供的方法。
从包含该同步区域的类的角度来看，这样的方法是外来的，这个类不知道该方法会有什么事情，也无法控制它。
根据外来方法的作用，从同步区域中调用它会导致异常、死锁或者数据损坏。

如果一个可变的类要并发使用，应该使这个类变成是线程安全的，若通过内部同步，还可以获得明显比外部锁定整个对象更高的并发性。
否则，就不要在内部同步。让客户在必要的时候从外部同步。

除了不要从同步区域内部调用外来方法，还要尽量限制同步区域内部的工作量。