尽可能使用stack变量


如果需要频繁的访问变量，就得仔细思考它们从哪里被访问。
JVM是一种stack-based虚拟机，因此在访问和操纵栈数据时性能最佳。

这并非意味着应该避免用 static 变量或 instance 变量，什么样的存储机制符合你的设计，就应当用什么样的变量。
如果在循环中访问 static 变量或 instance 变量，则可以将它们暂时存储于一个局部变量中，这样便能大幅提升程序性能。