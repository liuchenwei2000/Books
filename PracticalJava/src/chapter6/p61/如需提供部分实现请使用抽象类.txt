如需提供部分实现请使用抽象类


抽象类可以定义：
1，带有缺省实现代码的函数。
2，无任何实现代码的函数。

由于抽象类未必为其所有函数都提供完整实现，所以建立其实体是没有意义的，编译器也不会通过。

当打算提供只带部分实现的class时（也许这一部分实现对所有派生类都通用），抽象类非常有用。那些未被实现的函数（于是成为abstract）必然是因为“如果由基类提供实现代码则将缺乏实际意义，所以必须留待派生类去实现。
