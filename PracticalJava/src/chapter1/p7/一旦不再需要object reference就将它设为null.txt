一旦不再需要object reference就将它设为null


当不再需要某对象时，可以将其references设为null，以协助垃圾回收器回收内存。如果程序运行在一个内存受限的环境中，这会很有益处。

这种技术也可以用于函数中，它们的局部变量可能拥有短暂的生命。别以为函数结束时其局部对象不再被引用，所以不会影响到程序。

这种技术可以解决 大量内存被占用而它们其实不再有用的问题。为了尽量降低内存用量，与程序同寿的对象必须尽可能体积小。
此外，大块头对象应该尽量速生速灭。
