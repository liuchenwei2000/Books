synchronized修饰static函数与instance函数是有差异的


关键词synchronized可作用于static函数和class字面量(即Object.class这种对象)，就像作用于instance函数和对象上一样。
当调用一个synchronized static函数时，获得的lock将与定义该函数的class的Class对象相关联，而不是调用函数的那个对象相关联。
当对一个class字面量调用其synchronized区段时，获得的也是同样那个lock，也就是与特定Class对象相关联的lock。
